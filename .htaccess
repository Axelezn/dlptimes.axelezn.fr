# --- DÉBUT DE LA CONFIGURATION ---

# 1. Activation du moteur de réécriture d'URL
# Nécessaire pour masquer les extensions
RewriteEngine On

# Si vous n'êtes pas à la racine du domaine (ex: /monsite/), vous devez décommenter la ligne suivante :
# RewriteBase /

# ----------------------------------------------------
# 2. MASQUER LES EXTENSIONS (.HTML)
# ----------------------------------------------------

# Condition pour ignorer les requêtes vers des fichiers qui existent déjà (images, css, js)
# et ignorer les requêtes vers un dossier qui existe déjà.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Si l'URL demandée n'a PAS d'extension, et que le fichier .html correspondant existe
RewriteRule ^([^\.]+)$ $1.html [L]

# ----------------------------------------------------
# 3. OPTIMISATION DE LA PERFORMANCE (Compression Gzip/Deflate)
# ----------------------------------------------------

SetOutputFilter DEFLATE
AddOutputFilterByType DEFLATE "application/atom+xml" "application/javascript" "application/json" "application/ld+json" "application/manifest+json" "application/rdf+xml" "application/rss+xml" "application/schema+json" "application/vnd.geo+json" "application/vnd.ms-fontobject" "application/x-font-ttf" "application/x-javascript" "application/x-web-app-manifest+json" "application/xhtml+xml" "application/xml" "font/eot" "font/opentype" "image/bmp" "image/svg+xml" "image/vnd.microsoft.icon" "image/x-icon" "text/cache-manifest" "text/css" "text/html" "text/javascript" "text/plain" "text/vcard" "text/vnd.rim.location.xloc" "text/vtt" "text/x-component" "text/x-cross-domain-policy" "text/xml"

# ----------------------------------------------------
# 4. SÉCURITÉ (Protection du Répertoire Git)
# ----------------------------------------------------

# SECTION BEGIN GIT PROTECTION
RedirectMatch 404 /\.git
# SECTION END GIT PROTECTION

# --- FIN DE LA CONFIGURATION ---